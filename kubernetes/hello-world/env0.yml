version: 1

deploy:
  steps:
    setupVariables:
      after: &login-k8s
        - mkdir -p ~/.kube
        - echo $KUBE_CONFIG > ~/.kube/config
        - kubectl config set-context aws --namespace=$NAMESPACE;
        - echo "$(sed 's/!!!NAMESPACE!!!/'"$NAMESPACE"'/g' namespace.yaml)" > namespace.yaml;
        - cat namespace.yaml
        - kubectl apply -k . --prune --dry-run=server -l env0-environment-id=410c3e88-56a8-4f56-9119-b9d00adea89b --server=https://localhost:8080


destroy:
  steps:
    setupVariables:
      after: *login-k8s
